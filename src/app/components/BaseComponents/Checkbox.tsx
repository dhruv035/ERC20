import { Dispatch, SetStateAction } from "react";

const Checkbox = ({
  isOpen,
  setIsOpen,
  className,
}: {
  isOpen: boolean;
  setIsOpen: Dispatch<SetStateAction<boolean>>;
  className?: string;
}) => {
  return <div></div>;
};
